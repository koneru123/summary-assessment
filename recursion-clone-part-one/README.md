## Recursion Clone: Part One
Implement the function `copyArray` which has the following behavior:

* [ ] clones a nested array.
* [ ] returns a deep copy of the input array
* [ ] Uses the recursion pattern to solve this prompt
